version: "2"

formatters:
  enable:
    - gofmt

linters:
  default: none
  enable:
    - govet
    - gocritic
    - unused
    - ineffassign
    - bodyclose
    - nilerr
    - errcheck
    - staticcheck

  exclusions:
    rules:
      - path: _test\.go
        linters:
          - bodyclose
          - errcheck
